<Page x:Class="RubexOps.ViewEditProductionDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      Title="Production Control Center"
      Background="#F8FAFC"
      Loaded="Page_Loaded"
      d:DesignHeight="1000" d:DesignWidth="1600">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <SolidColorBrush x:Key="SoftTeal" Color="#0F766E" />
        <SolidColorBrush x:Key="SoftTealLight" Color="#CCFBF1" />
        <SolidColorBrush x:Key="SoftTealDark" Color="#115E59" />
        <SolidColorBrush x:Key="BurntOrange" Color="#EA580C" />
        <SolidColorBrush x:Key="BurntOrangeLight" Color="#FFEDD5" />
        <SolidColorBrush x:Key="DangerRed" Color="#EF4444" />
        <SolidColorBrush x:Key="SuccessGreen" Color="#10B981" />
        <SolidColorBrush x:Key="TextPrimary" Color="#0F172A" />
        <SolidColorBrush x:Key="TextMuted" Color="#64748B" />
        <SolidColorBrush x:Key="BorderSubtle" Color="#E2E8F0" />
        <SolidColorBrush x:Key="SurfaceWhite" Color="#FFFFFF" />

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style x:Key="CustomSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border" CornerRadius="12" Background="#CBD5E1">
                            <Ellipse x:Name="Knob" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="2,0,0,0">
                                <Ellipse.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SoftTeal}" />
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="Knob" Property="Margin" Value="0,0,2,0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InspectorCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceWhite}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="24" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="4" Direction="270" Opacity="0.04" Color="#0F172A" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedInputContainer" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceWhite}" />
            <Setter Property="BorderBrush" Value="#94A3B8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Height" Value="42" />
        </Style>

        <Style x:Key="SoftInputStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ReadOnlyInputStyle" TargetType="TextBox" BasedOn="{StaticResource SoftInputStyle}">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
            <Setter Property="Focusable" Value="False" />
        </Style>

        <Style x:Key="PrimaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SoftTeal}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryBtnStyle" TargetType="Button" BasedOn="{StaticResource PrimaryBtnStyle}">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="BatchTimelineTemplate">
            <Border Background="Transparent" Padding="0,4" Cursor="Hand" MouseLeftButtonUp="BatchNode_Click">
                <Grid Margin="0,0,12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="32" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Width="2" HorizontalAlignment="Center" Background="#CBD5E1" Margin="0,24,0,-24" />
                    <Border Width="16" Height="16" CornerRadius="8" Background="{StaticResource SurfaceWhite}" BorderBrush="{StaticResource SoftTeal}" BorderThickness="4" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,24,0,0" />

                    <Border Grid.Column="1" Background="{Binding BackgroundBrush}" BorderBrush="{Binding BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,8,0,8">
                        <StackPanel>
                            <TextBlock Text="{Binding BatchId}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource SoftTealDark}" />
                            <TextBlock Text="{Binding InvoiceDisplay}" Foreground="{StaticResource TextPrimary}" FontSize="13" Margin="0,4,0,0" />
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <materialDesign:PackIcon Kind="CalendarClock" Width="14" Height="14" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding DateDisplay}" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="6,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Border Background="{StaticResource SoftTealLight}" CornerRadius="4" Padding="6,2">
                                    <TextBlock Text="{Binding YieldDisplay}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource SoftTealDark}"/>
                                </Border>
                                <TextBlock Text="{Binding OutputDisplay}" FontWeight="SemiBold" Foreground="{StaticResource BurntOrange}" FontSize="13" Margin="12,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceWhite}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="View / Edit Production Data" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" />
                        <TextBlock Text="Production Batch Control Center" FontSize="14" Foreground="{StaticResource TextMuted}" Margin="0,4,0,0" />

                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0" VerticalAlignment="Center">
                            <Border Background="{StaticResource SoftTealLight}" CornerRadius="6" Padding="10,6">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="WaterOpacity" Foreground="{StaticResource SoftTealDark}" VerticalAlignment="Center" />
                                    <TextBlock Text="NRFC Input" FontWeight="Bold" Foreground="{StaticResource SoftTealDark}" Margin="6,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <materialDesign:PackIcon Kind="ChevronDoubleRight" Foreground="{StaticResource BorderSubtle}" Width="24" Height="24" Margin="12,0" VerticalAlignment="Center" />
                            <Border Background="#F1F5F9" CornerRadius="6" Padding="12,6">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Factory" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" />
                                    <TextBlock Text="Processing" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="6,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <materialDesign:PackIcon Kind="ChevronDoubleRight" Foreground="{StaticResource BorderSubtle}" Width="24" Height="24" Margin="12,0" VerticalAlignment="Center" />
                            <Border Background="{StaticResource BurntOrangeLight}" CornerRadius="6" Padding="10,6">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Layers" Foreground="{StaticResource BurntOrange}" VerticalAlignment="Center" />
                                    <TextBlock Text="ISNR20 Output" FontWeight="Bold" Foreground="{StaticResource BurntOrange}" Margin="6,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="8" Padding="16,10" Margin="0,0,12,0">
                            <StackPanel>
                                <TextBlock Text="Average Yield" FontSize="12" Foreground="{StaticResource TextMuted}" FontWeight="SemiBold" />
                                <TextBlock x:Name="KpiYieldText" Text="0.00%" FontSize="22" FontWeight="Bold" Foreground="{StaticResource SoftTeal}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                        <Border BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="8" Padding="16,10" Margin="0,0,12,0">
                            <StackPanel>
                                <TextBlock Text="Total Output" FontSize="12" Foreground="{StaticResource TextMuted}" FontWeight="SemiBold" />
                                <TextBlock x:Name="KpiOutputText" Text="0.00" FontSize="22" FontWeight="Bold" Foreground="{StaticResource BurntOrange}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                        <Border BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="8" Padding="16,10">
                            <StackPanel>
                                <TextBlock Text="Total Batches" FontSize="12" Foreground="{StaticResource TextMuted}" FontWeight="SemiBold" />
                                <TextBlock x:Name="TotalBatchesText" Text="0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="380" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Background="#F1F5F9" CornerRadius="8" Padding="12,0" Height="42">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Magnify" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" />
                            <TextBox x:Name="SearchBox" Grid.Column="1" Background="Transparent" BorderThickness="0" Margin="8,0,0,0" VerticalContentAlignment="Center" TextChanged="SearchBox_TextChanged" KeyDown="SearchBox_KeyDown" />
                            <TextBlock x:Name="SearchPlaceholder" Grid.Column="1" Text="Search Batch / Invoice / Vendor (Prefix)..." Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False" />
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource InspectorCardStyle}" Padding="16,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Margin="4,0,0,24">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ViewStream" Width="20" Height="20" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" />
                            <TextBlock Text="Batch Stream" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="Kg" FontWeight="Bold" Foreground="{StaticResource SoftTealDark}" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <ToggleButton x:Name="UnitToggleButton" Style="{StaticResource CustomSwitchStyle}" Checked="UnitToggleButton_CheckedChanged" Unchecked="UnitToggleButton_CheckedChanged" />
                            <TextBlock Text="MT" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="8,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>

                    <ListBox x:Name="BatchStreamListBox" Grid.Row="1" ItemTemplate="{StaticResource BatchTimelineTemplate}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="BatchStreamListBox_SelectionChanged" />
                </Grid>
            </Border>

            <Border x:Name="InspectorPanel" Grid.Column="2" Style="{StaticResource InspectorCardStyle}" Visibility="Hidden">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ShieldCheck" Foreground="{StaticResource SoftTeal}" Width="18" Height="18" VerticalAlignment="Center"/>
                                <TextBlock Text="Batch Inspector" FontSize="14" Foreground="{StaticResource SoftTeal}" FontWeight="Bold" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock x:Name="InspectorBatchIdText" Text="-" FontSize="36" FontWeight="Black" Foreground="{StaticResource TextPrimary}" Margin="0,4,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="Status: Confirmed" Foreground="{StaticResource SuccessGreen}" FontWeight="Bold" FontSize="13" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                            <TextBlock Text="Lock: Immutability Enforced" Foreground="{StaticResource TextMuted}" FontSize="12" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>

                    <Separator Grid.Row="1" Background="{StaticResource BorderSubtle}" Margin="-24,0,-24,24" />

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="1.2*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <materialDesign:PackIcon Kind="LockOutline" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                <TextBlock Text="Source (Purchase) Details" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <UniformGrid Columns="1" VerticalAlignment="Top">
                                <StackPanel Margin="0,0,0,16">
                                    <TextBlock Text="Vendor Identity" Style="{StaticResource FieldLabelStyle}" />
                                    <Border Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                        <TextBox x:Name="InspectorVendorNameText" Style="{StaticResource ReadOnlyInputStyle}" />
                                    </Border>
                                </StackPanel>

                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Contract ID" Style="{StaticResource FieldLabelStyle}" />
                                        <Border Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                            <TextBox x:Name="InspectorContractText" Style="{StaticResource ReadOnlyInputStyle}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Invoice Number" Style="{StaticResource FieldLabelStyle}" />
                                        <Border Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                            <TextBox x:Name="InspectorInvoiceText" Style="{StaticResource ReadOnlyInputStyle}" />
                                        </Border>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Purchase Date" Style="{StaticResource FieldLabelStyle}" />
                                        <Border Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                            <TextBox x:Name="InspectorPurchDateText" Style="{StaticResource ReadOnlyInputStyle}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Initial DRC" Style="{StaticResource FieldLabelStyle}" />
                                        <Border Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                            <TextBox x:Name="InspectorInitialDrcText" Style="{StaticResource ReadOnlyInputStyle}" Foreground="{StaticResource TextPrimary}" />
                                        </Border>
                                    </StackPanel>
                                </Grid>

                                <StackPanel>
                                    <TextBlock Text="Input Quantity (Net Weight)" Style="{StaticResource FieldLabelStyle}" />
                                    <Border Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                        <TextBox x:Name="InspectorInputQtyText" Style="{StaticResource ReadOnlyInputStyle}" FontSize="18" Foreground="{StaticResource SoftTealDark}" />
                                    </Border>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <Grid Margin="0,0,0,20">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <materialDesign:PackIcon Kind="PencilOutline" Foreground="{StaticResource BurntOrange}" VerticalAlignment="Center"/>
                                    <TextBlock Text="Production Execution" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="Edit Mode" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}" FontWeight="SemiBold" FontSize="13"/>
                                    <ToggleButton x:Name="EditModeToggle" Style="{StaticResource CustomSwitchStyle}" Checked="EditModeToggle_CheckedChanged" Unchecked="EditModeToggle_CheckedChanged" />
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Production Date" Style="{StaticResource FieldLabelStyle}" />
                                    <DatePicker x:Name="InspectorProductionDate" Height="42" IsEnabled="False" SelectedDateChanged="ProductionInput_Changed" />
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Output (ISNR20)" Style="{StaticResource FieldLabelStyle}" />
                                    <Grid>
                                        <Border x:Name="OutputBorderWrapper" Style="{StaticResource RoundedInputContainer}" Background="#F1F5F9" BorderThickness="0">
                                            <TextBox x:Name="InspectorOutputQty" Style="{StaticResource ReadOnlyInputStyle}" FontWeight="Bold" FontSize="18" Foreground="{StaticResource BurntOrange}" TextChanged="ProductionInput_Changed" Padding="12,0,40,0" />
                                        </Border>
                                        <TextBlock x:Name="UnitSuffixText" Text="kg" Foreground="{StaticResource TextMuted}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <Border Background="#F8FAFC" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" CornerRadius="8" Padding="20">
                                <StackPanel>
                                    <Grid Margin="0,0,0,16">
                                        <TextBlock Text="Live Batch Mathematics" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" />
                                        <TextBlock Text="Auto-Calculated" Foreground="{StaticResource TextMuted}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <TextBlock Text="Production Loss" Style="{StaticResource FieldLabelStyle}" Margin="0" />
                                        <TextBlock x:Name="CalcLossText" Text="-" HorizontalAlignment="Right" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" />
                                    </Grid>
                                    <Grid Margin="0,0,0,12">
                                        <TextBlock Text="Actual DRC (Yield %)" Style="{StaticResource FieldLabelStyle}" Margin="0" />
                                        <TextBlock x:Name="CalcActualDrcText" Text="-" HorizontalAlignment="Right" FontSize="15" FontWeight="Bold" Foreground="{StaticResource SoftTeal}" />
                                    </Grid>
                                    <Grid Margin="0,0,0,24">
                                        <TextBlock Text="DRC Variance" Style="{StaticResource FieldLabelStyle}" Margin="0" />
                                        <TextBlock x:Name="CalcVarianceText" Text="-" HorizontalAlignment="Right" FontSize="15" FontWeight="Bold" />
                                    </Grid>

                                    <TextBlock Text="DRC Performance Gauge" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Initial Test" FontSize="11" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock x:Name="GaugeInitialDrc" Text="0.00%" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                            <TextBlock Text="Actual Factory Yield" FontSize="11" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Right" />
                                            <TextBlock x:Name="GaugeActualDrc" Text="0.00%" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SoftTealDark}" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="0,12,0,0">
                                        <Border Height="8" CornerRadius="4">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                    <GradientStop Color="#EF4444" Offset="0.0" />
                                                    <GradientStop Color="#F59E0B" Offset="0.4" />
                                                    <GradientStop Color="#10B981" Offset="1.0" />
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>

                                        <Border x:Name="GaugeInitialMarker" Width="4" Height="16" Background="{StaticResource TextMuted}" CornerRadius="2" HorizontalAlignment="Left" Margin="100,0,0,0" ToolTip="Initial Purchase DRC" />
                                        <Ellipse x:Name="GaugePointer" Width="18" Height="18" Fill="{StaticResource SurfaceWhite}" Stroke="{StaticResource TextPrimary}" StrokeThickness="4" HorizontalAlignment="Left" Margin="150,0,0,0">
                                            <Ellipse.Effect>
                                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3" />
                                            </Ellipse.Effect>
                                        </Ellipse>
                                    </Grid>
                                    <Grid Margin="0,6,0,0">
                                        <TextBlock Text="85%" FontSize="10" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Left"/>
                                        <TextBlock Text="100%" FontSize="10" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <ItemsControl x:Name="ValidationList" Margin="0,20,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                            <materialDesign:PackIcon Kind="{Binding IconKind}" Foreground="{Binding IconColor}" Width="16" Height="16" VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding Message}" Foreground="{Binding TextColor}" FontSize="13" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="3" Margin="-24,24,-24,-24" Background="#F8FAFC" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,1,0,0" Padding="24,16">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="CancelEditButton" Content="Cancel Edit" Style="{StaticResource SecondaryBtnStyle}" Width="120" Margin="0,0,12,0" Visibility="Collapsed" Click="CancelEditButton_Click" />
                            <Button x:Name="SaveChangesButton" Content="Save Changes" Style="{StaticResource PrimaryBtnStyle}" Width="160" IsEnabled="False" Click="SaveChangesButton_Click" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>